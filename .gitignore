.DS_Store
**/.DS_Store
**/__pycache__/
*.pyc

# machine-local job file (rendered by bin/machine.sh) + tuning overrides
# wallet.local is tracked on purpose: every Mac mines to the same public address
com.minerv3.xmrig.plist
machine.local
flex.on
logs/*.log
logs/*.pid
logs/last-session.json
logs/last-session.json.tmp

# xmrig source checkout lives locally; GitHub ships bin/xmrig
vendor/

# extra local binaries / bundles
bin/xmrig.clang17
bin/xmrig.prebuilt
XMR-Miner-Portable.zip
